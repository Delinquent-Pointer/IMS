@page
@model IMS.Pages.InventoryManagerLandingModel
@{
  ViewData["Title"] = "Inventory Manager Dashboard";
}

<!-- Header with Log Out Button -->
<div class="d-flex justify-content-between align-items-center mb-4">
  <h1>Inventory Manager Dashboard</h1>
  <form method="post">
    <button type="submit" asp-page-handler="Logout" class="btn btn-danger">Log Out</button>
  </form>
</div>

@if (ViewData.ModelState[""]?.Errors?.Count > 0)
{
  <div class="alert alert-warning">
    @ViewData.ModelState[""]?.Errors?.FirstOrDefault()?.ErrorMessage
  </div>
}

<!-- Top Quick Info Bar -->
<div class="row mb-4">
  <div class="col-md-4">
    <h4>Weather</h4>
    <div id="weather-widget" class="border p-3">
      <p>Fetching Weather...</p>
    </div>
  </div>
  <div class="col-md-4 text-center">
    <h4>Current Date & Time</h4>
    <div class="border p-3">
      <p id="live-clock"></p>
    </div>
  </div>
  <div class="col-md-4 text-right">
    <h4>Quick Order</h4>
    <div id="quick-order" class="border p-3">
      <button class="btn btn-success">Order Products</button>
    </div>
  </div>
</div>

<!-- Quick Product Search -->
<div class="row mb-4">
  <div class="col-md-6">
    <h4>Quick Product Search</h4>
    <form method="get">
      <input type="text" name="SearchTerm" value="@Model.SearchTerm" class="form-control"
        placeholder="Search by name, description, category, SKU, etc..." />
      <button type="submit" class="btn btn-primary mt-2">Search</button>
    </form>
    @if (!string.IsNullOrEmpty(Model.SearchTerm) && Model.Products.Count > 0)
    {
      <ul class="list-group">
        @foreach (var product in Model.Products)
        {
          <li class="list-group-item">
            <strong>@product.Name</strong> - $@product.Price, Qty: @product.Quantity
          </li>
        }
      </ul>
    }
    else if (!string.IsNullOrEmpty(Model.SearchTerm))
    {
      <p>No products found.</p>
    }
  </div>
</div>

<!-- Notes Section -->
<div class="row mb-4">
  <div class="col-md-6">
    <h4>Notes</h4>
    <form method="post" asp-page-handler="AddNote">
      <textarea name="NewNote" class="form-control" placeholder="Enter note here..." rows="3"></textarea>
      <button type="submit" class="btn btn-secondary mt-2">Save Note</button>
    </form>
    <hr />
    <h6>Recent Notes</h6>
    @if (Model.Notes.Count > 0)
    {
      <ul class="list-group">
        @foreach (var note in Model.Notes)
        {
          <li class="list-group-item">
            <small>@note.Timestamp.ToString("f")</small> - @note.Content
          </li>
        }
      </ul>
    }
    else
    {
      <p>No notes yet.</p>
    }
  </div>
</div>

<!-- Interactive Calendar -->
<div class="calendar-container">
  <table class="calendar">
    <thead>
      <tr>
        <th>Sun</th>
        <th>Mon</th>
        <th>Tue</th>
        <th>Wed</th>
        <th>Thu</th>
        <th>Fri</th>
        <th>Sat</th>
      </tr>
    </thead>
    <tbody id="calendar-body"></tbody>
  </table>
</div>

<!-- Bootstrap Modal for Adding Events -->
<div class="modal fade" id="eventModal" tabindex="-1" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header bg-primary text-white">
        <h5 class="modal-title">Add Event</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
      </div>
      <div class="modal-body">
        <label>Event Title:</label>
        <input type="text" id="event-title" class="form-control" placeholder="Enter event name">
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
        <button id="save-event" class="btn btn-success">Save Event</button>
      </div>
    </div>
  </div>
</div>

@section Scripts {
  <script>
    function updateClock() {
      document.getElementById("live-clock").textContent = new Date().toLocaleString();
    }
    setInterval(updateClock, 1000);
    updateClock();

    fetch('https://api.weather.gov/gridpoints/SEW/124,67/forecast')
      .then(response => response.json())
      .then(data => {
        document.getElementById("weather-widget").innerHTML = `<p>${data.properties.periods[0].detailedForecast}</p>`;
      })
      .catch(() => document.getElementById("weather-widget").innerHTML = "<p>Weather not available.</p>");
  </script>
}
